// src/theme.d.ts

import { ThemeOptions } from '@mui/material/styles';

declare module '@mui/material/styles' {
  interface Theme {
    // Extend the theme as necessary
  }
  interface ThemeOptions {
    // Extend the theme options if necessary
  }
}
